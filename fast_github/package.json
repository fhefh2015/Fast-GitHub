{
  "name": "translation-machine",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "cross-env  NODE_ENV=production && tsc && vite build --watch",
    "dev:firefox": "yarn install && tsc && TARGET=firefox vite dev",
    "build": "cross-env NODE_ENV=production && tsc && vite build",
    "build:firefox": "yarn install && tsc && TARGET=firefox vite build",
    "validate": "web-ext lint -s dist"
  },
  "resolutions": {
    "react-error-overlay": "6.0.9"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.200",
    "@types/file-saver": "^2.0.5",
    "@types/react-dom": "^18.0.8",
    "@vitejs/plugin-react": "^2.2.0",
    "autoprefixer": "^10.4.13",
    "cross-env": "^7.0.3",
    "postcss": "^8.4.19",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-error-overlay": "6.0.9",
    "tailwindcss": "^3.2.3",
    "typescript": "^4.6.4",
    "vite": "^3.2.0",
    "vite-plugin-web-extension": "1.4.4"
  },
  "dependencies": {
    "@types/react": "^18.0.24",
    "file-saver": "^2.0.5",
    "terser": "^5.15.1"
  }
}
